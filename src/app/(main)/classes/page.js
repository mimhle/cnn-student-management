"use client";

const columns = [
    {
        title: "Id",
        dataIndex: "id",
        key: "id",
    },
];

export default function Page({ params }) {
    return (
        <div>
            <h1 className="text-3xl font-bold underline">
                classes
            </h1>
        </div>
    );
}